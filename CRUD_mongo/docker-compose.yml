version: '3.8'

services:
  mongodb:
    image: mongo:6.0
    container_name: idf-mongodb
    restart: always
    ports:
      - "27018:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
